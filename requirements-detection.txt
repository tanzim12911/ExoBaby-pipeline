# =============================================================================
# requirements-detection.txt — Detection pipeline dependencies (Colab / GPU)
# =============================================================================
# Covers: run_detection.py and the detection/ package
#
# Installed automatically by Step 0 of ExoBaby_LongTailed_Colab.ipynb.
# For local GPU machines: pip install -r requirements-detection.txt
#
# Only packages NOT pre-installed in Colab are listed here.
# Never pin numpy, scipy, pandas, matplotlib, tqdm, requests, Pillow,
# or huggingface_hub — Colab's pre-installed versions must be preserved
# to avoid binary incompatibility errors (numpy dtype size mismatch etc.)

ultralytics>=8.3
open-clip-torch>=2.20.0
powerlaw>=1.5
