# =============================================================================
# requirements-detection.txt — Detection pipeline dependencies (Colab / GPU)
# =============================================================================
# Covers: run_detection.py and the detection/ package
# (cdi_loader, detector, analysis, visualizer)
#
# These are installed automatically by the first cell in the Colab notebook.
# If you want to run locally on a GPU machine:
#   pip install -r requirements-detection.txt

ultralytics>=8.3
open-clip-torch==2.24.0
powerlaw==1.5
huggingface_hub==0.23.4
pandas==2.2.2
matplotlib==3.9.0
scipy==1.13.1
requests==2.32.3
numpy==1.26.4
tqdm==4.66.4
Pillow==10.4.0
